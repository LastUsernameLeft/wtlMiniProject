<h1><p class="text-center">Welcome to {{title}}!</p></h1>
<hr>
<div class="row">
    <div class="col-sm-6 col-md-4 col-md-offset-4 col-sm-offset-3">
        <div id="success" class="alert alert-success" {{#if noMessages}}hidden{{/if}}>
            {{ successMsg }}
        </div>
    </div>
</div>
<div class="flex-container">
    {{# each products }}
        {{> thumbnail }}
    {{/each}}
</div>